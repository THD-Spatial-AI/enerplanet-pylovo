# Pylovo Data Pipeline Requirements

# Core dependencies
requests>=2.28.0
pyyaml>=6.0

# Geospatial processing (optional, but recommended)
geopandas>=0.12.0
pandas>=1.5.0
shapely>=2.0.0

# Progress display
tqdm>=4.64.0

# Enrichment modules (optional, for Netherlands data)
cbsodata>=1.3.0

# Note: The following system tools are also required:
# - osmium-tool: sudo apt install osmium-tool
# - GDAL/ogr2ogr: sudo apt install gdal-bin
# - Java (for osm2po): sudo apt install default-jre
tqdm>=4.65.0
